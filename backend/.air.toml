root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/code-proxy ."
  bin = "./tmp/code-proxy"
  full_bin = "./tmp/code-proxy"
  include_ext = ["go", "toml"]
  exclude_dir = ["tmp", "embed"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
